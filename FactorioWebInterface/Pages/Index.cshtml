@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<form method="post" asp-page-handler="RemoveRoles">
    <div class="form-group">
        <button type="submit" class="btn btn-default">Remove Roles</button>
    </div>
</form>